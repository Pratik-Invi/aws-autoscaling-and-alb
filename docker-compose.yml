services:
  backend:
    build: ./backend
    restart: unless-stopped
    environment:
      PORT: "8080"
    expose:
      - "8080"

  frontend:
    build: ./frontend
    restart: unless-stopped
    depends_on:
      - backend
    ports:
      - "80:80"
